# 实验室自适应优化验证平台 MVP PRD

## 1. 产品定位

本 MVP 面向科研和工业实验室中的小样本、安全约束实验场景，提供一套从数据导入、特征提取、参数推荐、安全校验、实验记录到报告生成的闭环验证平台。

第一阶段聚焦 **SiC/GaN 关断振荡优化**，通过一个完整场景验证平台能力；第二阶段用 **轨道绝缘检测** 作为插件化扩展样例，验证平台能否低成本接入新实验类型。

产品核心价值：

- 降低实验参数探索成本。
- 提高实验过程的可追溯性。
- 在安全约束下推荐下一组候选参数。
- 将仿真、历史数据和真实实验结果统一管理。
- 为后续数字孪生、强化学习和多实验类型扩展打基础。

## 2. 目标用户

### 主要用户

- 电力电子实验室研究人员。
- SiC/GaN 器件应用工程师。
- 工业实验室测试工程师。
- 需要做参数优化和实验对比的科研团队。

### 用户痛点

- 实验数据分散，难以追踪每次参数配置和结果。
- 波形指标依赖人工观察，效率低且容易不一致。
- 参数调优依赖经验，试错成本高。
- 仿真数据和真实实验数据难以闭环对比。
- 实验报告整理耗时，复现实验困难。

## 3. MVP 目标

MVP 要跑通一条完整实验闭环：

1. 创建 SiC/GaN 实验项目。
2. 导入波形数据。
3. 自动提取核心指标。
4. 基于历史结果推荐下一组实验参数。
5. 对推荐参数进行安全约束检查。
6. 人工确认后记录执行结果。
7. 对比多次实验。
8. 自动生成 HTML 实验报告。

第一版不直接做真实设备全自动闭环控制，优化器只提供建议，所有建议必须经过安全层校验和人工确认。

## 4. MVP 范围

### 必须实现

- 实验项目管理。
- 实验 run 记录管理。
- CSV / JSON 数据导入。
- SiC/GaN 波形数据字段映射。
- 波形特征提取。
- 参数推荐。
- 安全约束检查。
- 实验结果对比。
- HTML 报告生成。
- 基础插件接口。

### 暂不实现

- 社区插件市场。
- 复杂动态热插拔插件系统。
- 真实设备无人值守自动控制。
- 强化学习实时控制。
- 完整多用户权限系统。
- 云端多租户部署。
- 多物理系统全面覆盖。

## 5. 主场景：SiC/GaN 关断振荡优化

### 输入数据

第一版建议支持以下字段：

- `time`：时间轴。
- `vgs`：栅极电压。
- `vds`：漏源电压。
- `ids`：漏极电流。
- `temperature`：温度，可选。

### 可调参数

- `dead_time`：死区时间。
- `gate_resistance`：栅极电阻。
- `drive_voltage`：驱动电压。
- `damping_resistance`：阻尼电阻，可选。

### 核心指标

- 最大 `vds`。
- 最大 `ids`。
- `vds` 过冲比例。
- 振荡频率。
- 振荡衰减时间。
- 稳态时间。
- 开关损耗估计。
- 风险等级。

### 优化目标

在满足安全约束的前提下：

- 降低关断过冲。
- 降低振铃幅度。
- 缩短振荡衰减时间。
- 控制开关损耗。
- 避免高温、高压、高电流风险。

## 6. 核心用户流程

1. 用户创建实验项目。
2. 用户选择实验类型：SiC/GaN 关断振荡。
3. 用户配置参数范围和安全阈值。
4. 用户导入历史实验数据或仿真数据。
5. 系统完成字段映射和数据校验。
6. 系统自动提取波形指标。
7. 系统展示波形、指标和异常点。
8. 系统根据历史 run 推荐下一组参数。
9. 安全层检查推荐参数。
10. 用户确认是否执行或记录该推荐。
11. 用户导入新实验结果。
12. 系统更新实验记录并生成对比报告。

## 7. 功能需求

### 7.1 项目管理

- 创建项目。
- 编辑项目名称、描述、实验类型。
- 查看项目下所有 run。
- 查看项目整体指标趋势。

### 7.2 实验配置

- 配置可调参数范围。
- 配置安全阈值。
- 配置优化目标权重。
- 保存配置版本。

### 7.3 数据导入

- 支持 CSV 导入。
- 支持 JSON 导入。
- 支持字段映射。
- 支持数据预览。
- 支持导入错误提示。

### 7.4 特征提取

- 自动识别峰值。
- 计算过冲比例。
- 估算振荡频率。
- 估算衰减时间。
- 计算风险等级。
- 保存指标结果。

### 7.5 参数推荐

- 基于历史 run 给出下一组候选参数。
- 展示推荐理由。
- 展示预期优化方向。
- 支持拒绝、接受、标记为已执行。

### 7.6 安全约束

- 参数上下限检查。
- 最大电压检查。
- 最大电流检查。
- 温度阈值检查。
- 禁止危险参数组合。
- 输出安全检查结果和拒绝原因。

### 7.7 实验对比

- 支持选择多个 run 对比。
- 对比参数配置。
- 对比核心指标。
- 对比波形曲线。
- 标记最佳结果。

### 7.8 报告生成

- 生成 HTML 报告。
- 包含项目基本信息。
- 包含实验参数和指标。
- 包含波形图。
- 包含优化前后对比。
- 包含风险提示。
- 包含下一步推荐参数。

## 8. 插件需求

第一版插件以标准接口为主，不追求复杂热插拔。

需要支持的插件类型：

- `DataSourcePlugin`：数据源插件。
- `FeaturePlugin`：特征提取插件。
- `OptimizerPlugin`：优化推荐插件。
- `ConstraintPlugin`：安全约束插件。
- `ReportPlugin`：报告插件。

第一版内置插件：

- `CSVDataSourcePlugin`
- `JSONDataSourcePlugin`
- `SiCGaNFeaturePlugin`
- `HeuristicOptimizerPlugin`
- `BayesianOptimizerPlugin`
- `SiCGaNConstraintPlugin`
- `HTMLReportPlugin`

## 9. 非功能需求

### 可追溯性

每次实验必须保存：

- 实验配置。
- 数据文件引用。
- 参数输入。
- 指标输出。
- 推荐参数。
- 安全校验结果。
- 用户确认状态。
- 报告版本。

### 安全性

- 优化器不得直接绕过安全层。
- 真实设备执行必须人工确认。
- 所有危险推荐必须记录拒绝原因。
- 安全阈值必须有默认值和配置版本。

### 可扩展性

- 新实验类型应尽量通过插件接入。
- 核心编排流程不应依赖具体物理系统。
- 指标、参数和约束应由实验插件声明。

## 10. MVP 验收标准

MVP 完成时应满足：

- 可以创建一个 SiC/GaN 实验项目。
- 可以导入 CSV 或 JSON 波形数据。
- 可以完成字段映射和数据校验。
- 可以自动提取至少 5 个核心指标。
- 可以记录每次实验 run。
- 可以根据历史 run 推荐下一组参数。
- 推荐参数必须经过安全约束检查。
- 超出约束的推荐会被拒绝并说明原因。
- 可以对比至少 2 次实验结果。
- 可以生成 HTML 实验报告。
- 可以用轨道绝缘检测样例验证基础插件扩展能力。

## 11. 版本路线

### V0.1

- 跑通本地数据导入、指标提取、run 记录。

### V0.2

- 加入参数推荐和安全约束。

### V0.3

- 加入前端可视化和实验对比。

### V0.4

- 加入报告生成和完整闭环。

### V0.5

- 加入第二实验类型插件样例。

